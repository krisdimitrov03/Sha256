#pragma once

const char WrongInputMessage[] = "Number incorrect. Select one of the options above.";

const int MAX_LETTER_BINARY_SIZE = 8;
const int MAX_WORD_BINARY_SIZE = 32;
const int MAX_CHUNK_SIZE = 64;